// Build-time stub for the optional "ai" (Vercel AI SDK) dependency inside the
// agents package. That import is only reached by client-side helper paths we
// never execute; aliasing it keeps the worker bundle self-contained.
export const jsonSchema = undefined;
